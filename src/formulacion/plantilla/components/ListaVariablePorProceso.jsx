const ListaVariablePorProceso = ({ procesoId }) => {
  return (
    <div>
      <h3>Lista de Variables asociadas al proceso {procesoId}</h3>
    </div>
  );
};

export default ListaVariablePorProceso;
